@model IEnumerable<MessageQueues.UI.Web.Models.ImageModel>


@foreach (var item in Model)
{
<p>
    @Url.Action("GetPreview", "Home", new { filePath = item.filePath })
</p>
<div>
    <img src=" @Url.Action("GetPreview", "Home", new { filePath = item.filePath })">
</div>
}
